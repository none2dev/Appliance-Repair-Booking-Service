# Use a minimal base image
FROM alpine:latest

# Set working directory
WORKDIR /app

# Copy source code (if any)
COPY . .

# Show environment variable from .env file
CMD echo "Hi, Just a echo test..." && tail -f /dev/null
